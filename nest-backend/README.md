NestJS scaffold for HSM booking (minimal)

1. Copy `.env.example` to `.env` and set DB / Razorpay credentials.
2. Install dependencies:

```bash
cd nest-backend
npm install
```

3. Start dev server:

```bash
npm run start:dev
```

This scaffold provides basic Booking entity, Razorpay service, and JWT stub.
